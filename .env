# Copy this file to .env and fill in your key.
# This is the ONLY external API call the app makes (structuring your spoken
# answers into résumé text). Speech-to-text, text-to-speech, and the header
# image all run on free/local services that need no key.
# Get a free key at https://console.groq.com/keys

import streamlit as st

GROQ_API_KEY= st.secrets["GROQ_API_KEY"]
