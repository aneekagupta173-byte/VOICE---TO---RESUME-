# This is a TEMPLATE only — never commit a real secrets.toml file.
# On Streamlit Community Cloud: go to your app's Settings > Secrets and
# paste the contents below (with your real key) into that panel instead.
# The app reads this key only from Streamlit Secrets, both locally and in the
# cloud. Copy this file to secrets.toml and replace the placeholder.

GEMINI_API_KEY = "your-key-here"
HF_TOKEN = "your-huggingface-read-token"
